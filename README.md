# Sample LayerZero V2 Hardhat Project

This project demonstrates a basic LayerZero V2 use case, using Hardhat.

Try running some of the following tasks:

```shell
yarn # or `npm install`
npx hardhat test
```
